/*
 * Consumer Data Standards
 * Sample Data Holder to Demonstrate the Consumer Data Right APIs
 *
 * NOTE: This class is auto generated by the cds-codegen package
 * https://github.com/ConsumerDataStandardsAustralia/cds-codegen
 * Do not edit the class manually.
 */
package au.org.consumerdatastandards.holder.model;

import org.hibernate.annotations.GenericGenerator;
import com.fasterxml.jackson.annotation.JsonCreator;
import com.fasterxml.jackson.annotation.JsonProperty;
import com.fasterxml.jackson.annotation.JsonValue;

import javax.persistence.*;
import java.util.Objects;
import java.lang.reflect.Field;

/**
 * CommonPhoneNumber
 * 
 * 
 */
@Entity
public class CommonPhoneNumber {

    @Id
    @GeneratedValue(generator = "system-uuid")
    @GenericGenerator(name = "system-uuid", strategy = "uuid2")
    private String phoneNumberId;

    @ManyToOne
    private CommonPersonDetail commonPersonDetail;

    // Required to be true for one and only one entry to indicate
    // the preferred phone number. Assumed to be 'false' if not
    // present
    @JsonProperty("isPreferred")
    private Boolean isPreferred;
    // The purpose of the number as specified by the customer
    @JsonProperty("purpose")
    private Purpose purpose;
    // If absent, assumed to be Australia (+61). The + should be
    // included
    @JsonProperty("countryCode")
    private String countryCode;
    // Required for non Mobile Phones, if field is present and
    // refers to Australian code - the leading 0 should be omitted.
    @JsonProperty("areaCode")
    private String areaCode;
    // The actual phone number, with leading zeros as appropriate
    @JsonProperty("number")
    private String number;
    // An extension number (if applicable)
    @JsonProperty("extension")
    private String extension;
    // Fully formatted phone number with country code, area code,
    // number and extension incorporated. Formatted according to
    // section 5.1.4. of [RFC
    // 3966](https://www.ietf.org/rfc/rfc3966.txt)
    @JsonProperty("fullNumber")
    private String fullNumber;

    public Boolean getIsPreferred() {
        return isPreferred;
    }

    public void setIsPreferred(Boolean isPreferred) {
        this.isPreferred = isPreferred;
    }

    public Purpose getPurpose() {
        return purpose;
    }

    public void setPurpose(Purpose purpose) {
        this.purpose = purpose;
    }

    public String getCountryCode() {
        return countryCode;
    }

    public void setCountryCode(String countryCode) {
        this.countryCode = countryCode;
    }

    public String getAreaCode() {
        return areaCode;
    }

    public void setAreaCode(String areaCode) {
        this.areaCode = areaCode;
    }

    public String getNumber() {
        return number;
    }

    public void setNumber(String number) {
        this.number = number;
    }

    public String getExtension() {
        return extension;
    }

    public void setExtension(String extension) {
        this.extension = extension;
    }

    public String getFullNumber() {
        return fullNumber;
    }

    public void setFullNumber(String fullNumber) {
        this.fullNumber = fullNumber;
    }

    @Override
    public boolean equals(java.lang.Object o) {
        if (this == o) {
            return true;
        }
        if (o == null || getClass() != o.getClass()) {
            return false;
        }

        CommonPhoneNumber inputModel = (CommonPhoneNumber) o;
        if (!(isPreferred.equals(inputModel.getIsPreferred()))) {
            return false;
        }
        if (!(purpose.equals(inputModel.getPurpose()))) {
            return false;
        }
        if (!(countryCode.equals(inputModel.getCountryCode()))) {
            return false;
        }
        if (!(areaCode.equals(inputModel.getAreaCode()))) {
            return false;
        }
        if (!(number.equals(inputModel.getNumber()))) {
            return false;
        }
        if (!(extension.equals(inputModel.getExtension()))) {
            return false;
        }
        if (!(fullNumber.equals(inputModel.getFullNumber()))) {
            return false;
        }
        return true;
    }

    @Override
    public int hashCode() {
        return Objects.hash(fullNumber, fullNumber, fullNumber, fullNumber, fullNumber, fullNumber, fullNumber);
    }

    @Override
    public String toString() {
        StringBuilder sb = new StringBuilder();
        sb.append(String.format("class %s {\n", getClass()));

        for (Field oneField : getClass().getFields()) {
            oneField.setAccessible(true);
            try {
                sb.append(String.format("    %s: %s\n", oneField.getName(), (oneField.get(Object.class) == null ? "null"
                        : oneField.get(Object.class).toString().replace("\n", "\n    "))));
            } catch (IllegalArgumentException | IllegalAccessException e) {
                // I guess we won't print it
                sb.append(String.format("    %s, [unreadable]\n", oneField.getName()));
            }
        }
        sb.append("}");
        return sb.toString();
    }

    /**
     * Purpose
     * 
     * 
     */
    public enum Purpose {
        MOBILE("MOBILE"), HOME("HOME"), WORK("WORK"), OTHER("OTHER"), INTERNATIONAL("INTERNATIONAL"),
        UNSPECIFIED("UNSPECIFIED");

        private String value;

        Purpose(String value) {
            this.value = value;
        }

        @Override
        @JsonValue
        public String toString() {
            return String.valueOf(value);
        }

        @JsonCreator
        public static Purpose fromValue(String value) {
            for (Purpose b : Purpose.values()) {
                if (b.value.equals(value)) {
                    return b;
                }
            }
            throw new IllegalArgumentException("Unexpected value '" + value + "'");
        }
    }

}
